const config = {
  content: ["./src/**/*.{html,js,svelte,ts}"],

  theme: {
    extend: {
      fontFamily: {
        'digi': ['DS-DIGI', 'sans-serif']
      }
    },
  },

  plugins: [],
};

module.exports = config;
